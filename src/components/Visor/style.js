import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
    visor: {
        color: 'white',
        textAlign: 'right',
        borderWidth: 0,
        padding: 5,
        marginRight: 20,
        fontSize: 40,
    },
    inputContainer: {
        marginTop: 70,
        borderBottomWidth: 0,
    }
})

export default style